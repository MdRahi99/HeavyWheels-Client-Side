import React from 'react';
import Title from "../../../../Hooks/Title";

const AddProducts = () => {
    Title('Add Products');
    return (
        <div>
            
        </div>
    );
};

export default AddProducts;